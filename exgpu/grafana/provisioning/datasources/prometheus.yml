apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus-exgpu
    url: http://prometheus:9090
    isDefault: true
    access: proxy
    jsonData:
      timeInterval: "15s"
